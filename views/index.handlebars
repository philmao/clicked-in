<style media="screen">
    .long {
        min-height: 400px;
    }
</style>

<div class="main main-raised">
    <div class="long">
        <div class="container">
            <div class="row">
                {{#each profiles}}
                    <a href="/viewprofile/{{id}}">
                        {{> profile-parts/profile }}
                    </a>
                {{/each}}
            </div>
        </div>
    </div>
</div>
