<style media="screen">
    .long {
        height: 1000px;
    }
</style>

<div class="long">
    <div class="container">
        <div class="row">
            {{#each profiles}}
                {{> profile-parts/profile}}
            {{/each}}
        </div>
    </div>
</div>

{{user.id}}
{{user.displayName}}
