<style media="screen">
    .long {
        height: 1000px;
    }
</style>

<div class="main main-raised">
    <div class="long">
        <div class="container">
            <div class="row">
                {{#each profiles}}
                    <a href="/viewprofile/{{this.id}}">
                        {{> profile-parts/profile}}
                    </a>
                {{/each}}
            </div>
        </div>
    </div>
</div>
